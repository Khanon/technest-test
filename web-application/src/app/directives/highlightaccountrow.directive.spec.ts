import { HighlightAccountRowDirective } from './highlightaccountrow.directive';

describe('HighlightAccountRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightAccountRowDirective();
    expect(directive).toBeTruthy();
  });
});
