import { HighlightTopBarDirective } from './highlighttopbar.directive';

describe('HighlightTopBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTopBarDirective();
    expect(directive).toBeTruthy();
  });
});
